name: Pull Request

on:
  pull_request:
    paths-ignore:
      - '*.md'
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual run'
        required: false

jobs:
  build:
    uses: kyhule/polyworld-workflows/.github/workflows/pre-merge.yml@dcd13b2607582b3527dc6d0a11a6d56b8020aac2
    secrets: inherit
